# Hiro Client Content

Public manifest and news feed for Hiro Client. The launcher reads
`manifest.json` and `news.json` from this repository; the client JAR is hosted
externally and referenced by the manifest.
